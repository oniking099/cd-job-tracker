# Core dependencies
# playwright 版本与 camoufox 强绑定：camoufox 0.4.x + playwright>=1.60 崩溃（协议事件），
# 0.5.4 已修复。pin 到本地实测通过的组合，避免 CI 每日装到漂移版本静默失败。
playwright==1.60.0
# Camoufox 反检测浏览器（BOSS/鱼泡等 CDP 检测墙平台），依赖未 pin playwright 须显式锁定
camoufox>=0.5.4,<0.6.0
playwright-stealth>=2.0.3
selectolax>=0.3.21
httpx>=0.27.0
jinja2>=3.1.4
python-dotenv>=1.0.0
pydantic>=2.0.0
openai>=1.0.0
python-Levenshtein>=0.25.0
google-generativeai>=0.8.0

# OCR 离线提取（视觉 LLM 兜底）
rapidocr>=3.0.0
onnxruntime>=1.18.0

# Dev
pytest>=8.0.0
pytest-asyncio>=0.24.0
